def get_move(data):
	
	get_move('direction')
	head = ['you']['body']
	x = head['x']
	y = head['y']
	left = x-1, y
	up = x, y-1
	down = x, y+1
	right = x+1, y

	("snake") = (p)
	if (p) == x-1, y:
		right
	if (p) == x+1, y:
		left
	if (p) is == x, y-1:
		down
	if (p) is == x, y+1:
		up
		
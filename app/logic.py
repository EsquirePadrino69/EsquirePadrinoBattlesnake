def get_move(data):
	
	get_move('direction')
	head = ['you']['body']
	x = head['x']
	y = head['y']
	left = x-1
	up = y-1
	down = y+1
	right = x+1

	("snake") = (p)
	if (p) is x-1
		else: right
	if (p) is x+1
		else: left
	if (p) is y-1
		else: down
	if (p) is y+1
		else: up
		
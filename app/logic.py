def get_move(data):
	
	get_move('direction')
	head = ['you']['body']
	x = head['x']
	y = head['y']
	head_left = (x-1, y)
	head_up = (x, y-1)
	head_down = (x, y+1)
	head_right = (x+1, y)

	("snake") = (p)
	if  (p) is (x-1, y) head_right
	else (p) is (x+1, y) head_left
	else (p) is (x, y-1) head_down
	else (p) is (x, y+1) head_up
		
		
def get_move(data):
	
	get_move('direction')
	head = ['you']['body']
	x = head['x']
	y = head['y']
	left = (x-1, y)
	up = (x, y-1)
	down = (x, y+1)
	right = (x+1, y)

	("snake") = (p)
	if  (p) is (x-1, y) right
	else (p) is (x+1, y) left
	else (p) is (x, y-1) down
	else (p) is (x, y+1) up
		
		
def get_move(data):
	
	get_move('direction')
	head = ['you']['body']
	x = head['x']
	y = head['y']
	left = x-1, y
	up = x, y-1
	down = x, y+1
	right = x+1, y

	("snake") = (p)
	if (p) is x-1, y
		else: right
	if (p) is x+1, y
		else: left
	if (p) is x, y-1
		else: down
	if (p) is x, y+1
		else: up
		